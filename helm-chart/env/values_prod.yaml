stage: prod
deployment: full
mfrabbitmq:
  user: user
  pw: vulkan
  port_ext: 30041
  adminport_ext: 30042
mfmongo:
  db_pw: vulkan
  db_port_ext: 30043
mfinstruments:
  mf_http_port_ext: 30044
mftransactions:
  mf_http_port_ext: 30045
mfmarketdata:
  mf_http_port_ext: 30046
mfvaluation:
  mf_http_port_ext: 30047
mfcomposite:
  mf_http_port_ext: 30049
mfshell:
  # ┌───────────── minute (0 - 59)
  # │ ┌───────────── hour (0 - 23)
  # │ │ ┌───────────── day of month (1 - 31)
  # │ │ │ ┌───────────── month (1 - 12)
  # │ │ │ │ ┌───────────── day of week (0 - 6) (Sunday to Saturday;
  # │ │ │ │ │                                       7 is also Sunday on some systems)
  # │ │ │ │ │
  # │ │ │ │ │
  # * * * * *  command to execute
  priceimportschedule: "0 10 * * *"
  processtransactionschedule: "5 10 * * *"