{{ if eq .Values.deployment "full" }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mfpriceimport
spec:
  schedule: "{{ .Values.mfshell.importprevcloseschedule }}"
  failedJobsHistoryLimit: 1
  successfulJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ .Values.mfshell.name }}
              image: "{{ .Values.image.repository }}mfshell:{{ .Values.mfshell.imageversion }}"
              args: ["importPrevClose"]
              env:
                - name: "spring.rabbitmq.port"
                  value: "{{ .Values.mfrabbitmq.port_ext }}"
                - name: "job.name"
                  value: "importPrevClose"
          restartPolicy: OnFailure
      backoffLimit: 4
{{ end }}