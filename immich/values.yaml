# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

immich:
  persistence:
    librarySize: 100Gi
    dbSize: 10Gi
  db:
    name: immich-db
    pvc: immich-db-pvc
    user: postgres
    database: immich
  redis:
    name: immich-redis
  ml:
    name: immich-ml
  app:
    name: immich
    pvc: immich-pvc
  oauth:
    clientSecret: vulkan
