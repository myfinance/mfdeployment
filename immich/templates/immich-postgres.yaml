{{- if .Values.immich.persistence.createPvc | default true }}
{{- if .Values.immich.persistence.dbVolumeName }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.immich.persistence.dbVolumeName }}
spec:
  capacity:
    storage: {{ .Values.immich.persistence.dbSize }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ .Values.immich.persistence.storageClassName }}
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: {{ .Values.immich.persistence.dbVolumeName }}
  claimRef:
    namespace: {{ .Release.Namespace }}
    name: {{ .Values.immich.db.pvc }}
---
{{- end }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.immich.db.pvc }}
  labels:
    app: {{ .Values.immich.db.name }}
spec:
  storageClassName: {{ .Values.immich.persistence.storageClassName }}
  {{- if .Values.immich.persistence.dbVolumeName }}
  volumeName: {{ .Values.immich.persistence.dbVolumeName }}
  {{- end }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.immich.persistence.dbSize }}

---
{{- end }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.immich.db.name }}
  labels:
    app: {{ .Values.immich.db.name }}
spec:
  strategy:
    type: Recreate
  replicas: {{ .Values.immich.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.immich.db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.immich.db.name }}
    spec:
      securityContext:
        fsGroup: 999
      containers:
        - name: postgres
          image: {{ .Values.immich.pgimage }}
          env:
            - name: POSTGRES_DB
              value: "{{ .Values.immich.db.database }}"
            - name: POSTGRES_USER
              value: "{{ .Values.immich.db.user }}"
            - name: POSTGRES_PASSWORD
              value: "{{ .Values.immich.db.pw }}"
            - name: POSTGRES_INITDB_ARGS
              value: "--data-checksums"
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
              name: dbport
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: db-data
          persistentVolumeClaim:
            claimName: {{ .Values.immich.db.pvc }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.immich.db.name }}
  labels:
    app: {{ .Values.immich.db.name }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: {{ .Values.immich.db.name }}
