apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mfmongo.db_name }}
spec:
  #this defines which pod is going to be presented by this service
  selector:
    app: {{ .Values.mfmongo.db_name }}
  ports:
    - name: {{ .Values.mfmongo.db_name }}
      port: {{ .Values.mfmongo.db_port }}
      targetPort: {{ .Values.mfmongo.db_port }}
  type: ClusterIP

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mfmongo{{ .Values.stage }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: "mfmongo{{ .Values.stage }}.{{ .Values.master_ip }}.nip.io"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.mfmongo.db_name }}
                port:
                  number: {{ .Values.mfmongo.db_port }}  