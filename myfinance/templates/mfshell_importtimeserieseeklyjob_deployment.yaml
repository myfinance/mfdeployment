apiVersion: batch/v1
kind: CronJob
metadata:
  name: mfpriceimport
spec:
  schedule: "{{ .Values.mfshell.importtimeseriesweeklyschedule }}"
  failedJobsHistoryLimit: 1
  successfulJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ .Values.mfshell.name }}
              image: "{{ .Values.image.repository }}mfshell:{{ .Values.mfshell.imageversion }}"
              args: ["importTimeseriesWeekly"]
              env:
                - name: "spring.rabbitmq.port"
                  value: "{{ .Values.mfrabbitmq.port }}"
                - name: "job.name"
                  value: "importTimeseriesWeekly"
          restartPolicy: OnFailure
      backoffLimit: 4
